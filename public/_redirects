# Host/scheme redirects (www->apex, http->https) are NOT configured here.
# The Cloudflare Workers _redirects parser only accepts relative-path
# redirects, so cross-host/scheme rules are set up as Cloudflare zone
# Redirect Rules + the "Always Use HTTPS" setting instead.
